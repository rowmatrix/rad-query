# rad-rag dependencies
# Install with: pip install -r requirements.txt

# PDF parsing (at least one required)
pdfplumber>=0.11
pymupdf>=1.24          # optional fallback (fitz)

# Embeddings
sentence-transformers>=3.0

# Vector store
chromadb>=0.5

# LLM backends (install whichever you use)
openai>=1.30           # for OpenAI GPT models
anthropic>=0.28        # for Claude models

# UI
streamlit>=1.35

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
